217. Contains Duplicate
Easy

Given an array of integers, find if the array contains any duplicates.

Your function should return true if any value appears at least twice in the array, and it should return false if every element is distinct.

Example 1:

Input: [1,2,3,1]
Output: true
Example 2:

Input: [1,2,3,4]
Output: false
Example 3:

Input: [1,1,1,3,3,4,3,2,4,2]
Output: true


```python
class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """

```

Runtime: 100 ms, faster than 90.88% of Python online submissions for Contains Duplicate.
Memory Usage: 17.2 MB, less than 59.26% of Python online submissions for Contains Duplicate.
```python
def containsDuplicate(nums):
    return len(set(nums))!=len(nums)
```

Other solutions from the problem page are:
1. Naive Linear Search, O(n**2) run time
2. Sorting approach, O(nlogn) run time
3. Hash table approach, O(n) run time